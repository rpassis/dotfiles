name: arrowfield
root: ~/Documents/Freshmob/Clients/Arrowfield/Dev

windows:
  - api:
      root: ~/Documents/Freshmob/Clients/Arrowfield/Dev/bloodstock_server
      layout: main-horizontal
      panes:
        - sleep 5 && docker-compose exec api bash
        - atom-beta . && sleep 1 && hideapp "Atom Beta" && docker-compose up
        - tail -f log/development.log
        - cx ssh -s "Bloodstock AWS" -e dev Giraffe
  - ios:
      root: ~/Documents/Freshmob/Clients/Arrowfield/Dev/bloodstock_ios
      layout: main-horizontal
      panes:
        - #empty
        - #empty
